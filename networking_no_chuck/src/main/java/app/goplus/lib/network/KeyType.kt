package app.goplus.lib.network

interface KeyType {
    companion object {
        const val normal = "normal"
        const val reactive = "reactive"
        const val googleReactive = "google"
        const val simple = "simple"
    }
}
